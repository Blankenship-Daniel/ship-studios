---
description: Reference for Gemini/Google music generation (Lyria 3 clip/pro + RealTime, MP3/WAV 44.1kHz) — preview; not wired in this repo.
argument-hint: [topic, e.g. "lyria models" | "output format" | "realtime"]
---

Invoke the **gemini-music-generation** skill on `$ARGUMENTS`.

`$ARGUMENTS` is the question about generating music with Gemini/Lyria. The skill explains the
Lyria models and output formats and points to `docs/gemini-audio/music-generation.md`. Note:
music generation is NOT wired into stemmy-gemini — the skill includes the wire-up note.
