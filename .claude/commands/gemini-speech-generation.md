---
description: Reference for Gemini text-to-speech (voices, multi-speaker, PCM 24kHz output, style tags) — preview; not wired in this repo.
argument-hint: [topic, e.g. "voices" | "output format" | "wire it up"]
---

Invoke the **gemini-speech-generation** skill on `$ARGUMENTS`.

`$ARGUMENTS` is the question about Gemini TTS. The skill explains the speech-generation models,
voices, and output format, and points to `docs/gemini-audio/speech-generation.md`. Note: TTS is
NOT wired into stemmy-gemini (read-only server) — the skill includes the wire-up note.
