# VST plugins in ship-studios — the headless-safe reference

The `[[vst]]` skill suite runs your installed **third-party VST3 / Audio Unit effect** plugins
inside the pipeline via the `[L] apply-vst-chain` tool (Pedalboard, the `vst` extra) — fully
**offline & headless** (no DAW, no GUI, no audio device). This doc is the suite's ground truth:
what's safe to use, why, and the strong candidates per task.

## What "headless-safe" means

A plugin is **headless-safe** when it *instantiates in an unattended, no-GUI process* on this Mac —
so `apply-vst-chain` can load and render through it without a window, a dialog, or a hang. This was
established empirically: every installed VST3/AU was load-tested via Pedalboard in an isolated,
timeout-guarded subprocess. **779 of 944 loadable titles passed.**

⚠️ **This is a *load* probe — loads ≠ renders.** A title can load yet pass audio through unprocessed
(notably the UAD `.component` build) or ignore its parameters. Before trusting a plugin, confirm it
actually *processes* with `[[vst-verify]]` (a parameter-response probe). The list below over-counts.

- Full raw list: [`../../demo/headless-safe-titles.txt`](../../demo/headless-safe-titles.txt) (779 titles)
- Full table + the blocked set with reasons: [`../../demo/installed-plugins-headless-safe.md`](../../demo/installed-plugins-headless-safe.md)

**Skills must only ever suggest a plugin that appears in that list.** Confirm the on-disk path at
run time with `[L] list-vst-plugins {name_contains}` before calling `apply-vst-chain` — and for UADx
prefer the `uaudio_*.vst3` build. When in doubt a plugin renders (vs just loads), screen it with `[[vst-verify]]`.

## Caveats (carry these into every skill)

- **Non-deterministic & opt-in.** Unlike the pure-DSP tools, this loads an external binary whose
  output isn't reproducible across plugin versions. Pin versions; persist `dump_state` blobs.
- **A clean load ≠ a valid render.** A plugin can instantiate yet pass audio through or ignore its
  params (demo/unlicensed, or the UAD `.component` build). Always **re-measure detail** after applying —
  spectrum / crest / tilt, not just `changed:true`/LUFS; a **0.00 change = passthrough**. Screen with `[[vst-verify]]`.
  **Worked example:** `Tape J-37` (Arturia) loads, accepts & echoes every param, even reports latency — but
  **renders pure passthrough** (1 kHz sine @ drive 50 → 0.00 % THD; drive 0 ≡ drive 40 ≡ Bypassed bit-for-bit;
  Arturia ASC licensing self-bypasses headless). DAW-only → [[tape-j-37]].
- **UAD has two builds (critical).** `/Library/Audio/Plug-Ins/VST3/uaudio_*.vst3` (UADx native) **renders
  headless**; the `/Components/UAD ….component` and `/VST3/Universal Audio/UAD ….vst3` twins **pass audio
  through unprocessed** offline — they ignore params even when authorized. **Always load the `uaudio_*.vst3`
  path.** Candidate names below written `UAD …` mean the `uaudio_*` build.
- **Gain-stage analog units.** Tube comps / tape / consoles need ~+18 dB into the chain to engage;
  `apply-vst-chain` has no gain-stage → use `[[vst-preset]]` for chains that need drive.
- **Format/OS.** VST3 is cross-platform; **AU (`.component`) is macOS-only**. Prefer VST3 paths.
- **Blocked classes** (will *not* render unattended here): iLok-gated vendors not machine-authorized
  (**Slate Digital**, **Eiosis**, part of **iZotope**), **UAD-2 DSP** (needs Apollo hardware), and
  **UAD Spark** subscriptions (cloud/dongle). UA *perpetual native UADx* are fine — via `uaudio_*.vst3`.

## Strong candidates by task (all confirmed headless-safe on this Mac)

Pick from these first; fall back to the raw list. Names are exact (feed to `list-vst-plugins`).

| Task | Confirmed-safe candidates | Skill |
|---|---|---|
| **Channel strip / console** | `SSL 4K E` (SSL's SL 4000 E — Brown/Black/Orange EQ + grabby VCA comp; clean/weighty British console → measured deep-dive [[ssl-4k-e]]; renders headless, iLok-authorized here, re-verify elsewhere), `British Channel`, `bx_console SSL 4000 E`, `bx_console AMEK 200`, `SSL Native Channel Strip 2`, `UAD API Vision Channel Strip`, `UAD Neve 1073`, `KIT BB A5` (Blackbird API Legacy strip — punchy), `KIT BB N105 V2` (Blackbird Neve 8078/31105 — warm, 4-band), `KIT BB N73` (Blackbird Neve **1073** — warm/weighty, 3-band, fixed-12k HF shelf → measured deep-dive [[kit-bb-n73]]) — all three KIT render headless & iLok-authorized here, re-verify elsewhere, `UAD Helios Type 69` (UADx `uaudio_helios_type_69.vst3` — warm "rock" British preamp + passive inductor 3-band EQ, **no compressor**; renders headless & **no-iLok** → measured deep-dive [[helios-type-69]]), `UAD Manley VOXBOX` (UADx `uaudio_manley_voxbox.vst3` — Manley's all-tube VOXBOX vocal channel: tube pre + passive electro-optical compressor + passive Pultec/MEQ-5 3-band EQ + de-esser/opto-limiter; the smooth/open "hi-fi" **tube** channel → measured deep-dive [[manley-voxbox]]; renders headless, UADx native; **all 25 params are enums → harness only**, the audio path is **comp-FIRST → pre → EQ → de-ess** not the GUI order, the opto comp RAISES crest (levels body/opens transients), the Pultec EQ 0–10 dials are nonlinear PEAK-DIP-PEAK [no cuts/Q], and the de-ess's 5th "Limit" position is a smooth opto limiter not a brickwall), `UAD LA-6176 Channel Strip` (UADx `uaudio_la_6176.vst3` — the **610 tube preamp + 2-band shelf EQ + a switchable 1176-OR-LA-2A compressor** in one pass: UA's Bill Putnam Jr. Signature **6176** [610 + 1176] **plus** the plug-in's never-before **MODE switch** that swaps the compressor between the fast **1176** FET and the slow/gentle **LA-2A** opto → measured deep-dive [[la-6176]]; renders headless, UADx native [no iLok]; **a bare load already compresses [default Dyn In ~5 dB GR → bypass/`master_bypass` for a clean A/B], the `Mic` path is a high-gain mic pre that hits ~40 % THD on a line-level stem [use `Line`/`Hi-Z` on stems; `Mic`+−15 pad only for heavy colour], the EQ feeds the compressor detector [a boost drives more GR], the inactive mode's controls go inert, and all 26 params are enums incl. digit-prefixed `1176_*` → harness only**) | `[[vst-channel-strip]]` (SSL E: [[ssl-4k-e]]; UAD API: [[api-vision-channel-strip]]; KIT/Blackbird API: [[kit-bb-a5]]; KIT/Blackbird Neve: [[kit-bb-n105]] (31105, 4-band) / [[kit-bb-n73]] (1073, 3-band); UADx Helios: [[helios-type-69]]; Manley tube: [[manley-voxbox]]; UADx LA-6176 (610+EQ+1176/LA-2A): [[la-6176]]) |
| **EQ (vintage / surgical)** | `FabFilter Pro-Q 4` (24-band surgical + dynamic + Spectral Dynamics + Character → measured deep-dive [[fabfilter-pro-q-4]]; renders headless & **no-iLok**, but its 581-param surface needs the flatten-first preset harness, not the float dict), `Maag EQ4`, `UAD Pultec EQP-1A` (the iconic passive LC inductor + push-pull-tube **program EQ** — the **low-end trick** (boost+atten the same CPS = bump + low-mid scoop = big-but-tight bottom) + **air-without-fizz** (independent HF-boost vs HF-cut freqs); broad/musical, colours via its **curves not drive** (~0.01 % THD); load the UADx `uaudio_pultec_eqp-1a.vst3`, renders headless & iLok-account → measured deep-dive [[pultec-eqp-1a]]; **boost/atten/bandwidth knobs are 0–10 DIAL POSITIONS not dB and nonlinear, engaged-flat is never a true null (+1 dB makeup → loudness-match), and 5 of 12 params are string enums [`low_freq`/`high_freq`/`hf_atten_freq`/`enable`/`output`] → harness only**), `UAD Pultec MEQ-5` (the passive LC inductor + tube **MID-range EQ** of the Pultec Collection, ~200 Hz–7 kHz: LOW PEAK boost / DIP cut / HIGH PEAK boost + a clean ±12 dB output → measured deep-dive [[pultec-meq-5]]; load the UADx `uaudio_pultec_meq-5.vst3`, renders headless & iLok-account; **unlike the EQP-1A/Hitsville knobs the MEQ-5 0–10 dial is ROUGHLY dB** (LOW PEAK +10.7 / HIGH PEAK +8.8 / DIP saturates −11 dB max), boost+DIP at the same Hz FOCUSES not cancels, and the **three amount knobs set via the float dict but the freq selectors / `output` / `enable` are string enums → harness only**), `UAD Neve 1073`, `KIT BB N105 V2` (4-band Neve 31105 → [[kit-bb-n105]]), `UAD Helios Type 69` (passive inductor 10k-shelf / Peak-Trough mid / bass-cut + preamp drive, UADx no-iLok → [[helios-type-69]]), `UAD Hitsville EQ Mastering` (the Motown / Hitsville U.S.A. disk-mastering EQ — a broad/musical passive proportional-Q **graphic** EQ: 7 fixed bands × Dip/Peak, half-speed freqs, fixed 70 Hz/15 kHz Motown Filters, and TRUE **Mid/Side** → measured deep-dive [[hitsville-eq-mastering]]; load the UADx `uaudio_hitsville_eq_mastering.vst3`, renders headless & iLok-account, **all 53 params are enums + the global speed switch is inert headless → harness only, set per-band `lN_freq`**), `UAD Hitsville EQ` (the Motown / Hitsville U.S.A. **Studio EQ** — UA's single-channel 7-fixed-band passive proportional-Q **graphic** EQ, the channel counterpart to the Mastering twin: bands 50/130/320/800/2k/5k/12.5k each **±8 dB stepped in REAL dB** + a makeup GAIN + a 3-way **In/Out/Off** switch → measured deep-dive [[hitsville-eq]]; load the UADx `uaudio_hitsville_eq.vst3`, renders headless & iLok-account, a **clean EQ — 0 % THD** at mix level, proportional-Q tightens with gain, and — uniquely among the UAD/console deep-dives — **fully drivable from `apply-vst-chain`'s float dict** since the 7 band gains + GAIN are numeric; only `bypass`/`power` enums need the harness, and their defaults are already right), `UAD Pultec HLF-3C` (the Pultec/Pulse Techniques **passive high-pass + low-pass FILTER** — NOT a tonal EQ; load the UADx `uaudio_pultec_hlf-3c.vst3`, renders headless & iLok-account → measured deep-dive [[pultec-hlf-3c]]; **only 4 enum params (`low_cut`/`high_cut`/`enable`/`master_bypass`), a 0.000 % THD pure filter (no tone/colour — only subtracts), ~18 dB/oct passive slopes, and the standard `probe_plugin.py` FALSE-flags it as passthrough → verify with a real corner not the canned probe**), `UAD Manley Massive Passive` (UA's model of the **Manley Massive Passive** — a 2-channel **4-band-per-channel PARALLEL passive (LC inductor) + all-tube make-up** EQ (solid-state input buffer + all-tube make-up gain — NOT tube/MOSFET); the BROAD, musical "everything sounds better" mastering/bus EQ, the opposite end from the surgical Pro-Q → measured deep-dive [[manley-massive-passive]]; load the UADx `uaudio_manley_massive_passive.vst3` (standard — continuous, ±20 dB; the screenshot) or `…_massive_passive_m.vst3` (MST/mastering — stepped/detented for recall + ±2.5 dB L/R trim), renders headless & iLok-account here — NOT the `.component` twin, and **`probe_plugin.py` FALSE-flags passthrough because every band defaults to `OUT`** (engage a band + verify with a real boost); **the GAIN dial is nonlinear + BANDWIDTH-COUPLED [dB = gain × bw, not the dial], `bw` 1.0 = widest / 3.0 = narrowest+tallest, a SHELF + narrow `bw` grows a resonant corner ["the shelves aren't shelves"], the bands INTERACT [parallel, non-additive], it's a CLEAN EQ [≤0.25 % THD, subtle even-harmonic make-up colour, crest held], and all 51 params are enums [`enable`/`shape`/`lopass`/`hipass`/`ctrllink` strings] → harness only, set BOTH channels**), `EQP-1A` | `[[vst-eq]]` (Pultec EQP-1A: [[pultec-eqp-1a]]; Pultec MEQ-5 mids: [[pultec-meq-5]]; Hitsville: [[hitsville-eq]]; passive filter: [[pultec-hlf-3c]]; Manley Massive Passive [broad/parallel-passive]: [[manley-massive-passive]]) / `[[vst-master]]` ([[hitsville-eq-mastering]]) |
| **Compressor / dynamics** | `FabFilter Pro-MB` (6-band MULTIBAND dynamics — compression AND expansion, up & down, on free-placed "think bands, not crossovers" bands with per-band side-chain + M/S → measured deep-dive [[fabfilter-pro-mb]]; renders headless & **no-iLok**; **RANGE is the master amount — `range = 0` is a bit-exact passthrough**, and a bare load is a TRUE passthrough so, unlike Pro-Q, you do NOT flatten; all 156 params are enums incl. `band_N_state`/`dynamics_mode`/`ratio` → **harness-only, the float dict can't enable a band**; attack/release are 0–100 % not ms), `Black 76`, `Comp FET-76`, `FabFilter Pro-C 2`, `SSL Native Bus Compressor 2` (SSL G-series VCA bus glue → measured deep-dive [[ssl-bus-compressor-2]]; renders headless, iLok-authorized here — re-verify elsewhere), `UAD Fairchild 660` (vari-mu TUBE color/glue — *colors more than it crushes*, low-mid thickening with crest held → measured deep-dive [[fairchild-660]]; load the `uaudio_fairchild_660.vst3` build, renders headless & no-iLok lineage; **all 12 params are enums → harness-only**; the 670 twin adds stereo / Lat-Vert M/S), `UAD Teletronix LA-3A` (solid-state electro-optical (T4) **LEVELER** — *reduces* crest [17.2→12.8] unlike the crest-holding tube Fairchild, and colours with **odd/3rd-harmonic grit** not even-harmonic tube warmth [~0.37 % THD only while compressing] → measured deep-dive [[la-3a]]; load the `uaudio_la3a.vst3` build, renders headless & no-iLok lineage; **HF Emphasis = a built-in sidechain HPF** [raise it to keep the kick punching], Peak Reduction has a dead zone ~0–2, and `comp_limit`/`meter` are string enums → harness for Limit mode), `UAD dbx 160` (the feed-forward, true-RMS, **hard-knee Blackmer-VCA** comp — UA's model of dbx's 1976 160 "VU"; on drums it **ADDS punch — it RAISES crest** [4:1 → 17.2→19.3, ∞:1 → 22.4 on our drum bus], the opposite of the crest-*dropping* opto LA-3A and the crest-*holding* tube Fairchild, and an exceptionally **clean VCA** [THD ~0.04 % working — its character is dynamic, not harmonic] → measured deep-dive [[dbx-160]]; load the `uaudio_dbx_160.vst3` build, renders headless & no-iLok lineage [the `/Components/UAD dbx 160.component` + `Universal Audio/.../UAD dbx 160.vst3` twins pass through offline]; **no attack/release** [program-dependent RMS], **the ratio `compress` is a STRING enum (`' 4.0:1'`/`'Inf:1'`) + `meter`/`sc_filter` are string/bool → harness only** [only thresh/gain/mix are numeric], `sc_filter` (PULL/SC) is a detector HPF that INVERTS the character [crest down + bassier = de-harsh, not punch], and it models the **hard-knee VU — no OverEasy, no negative/INFINITY+** ratio), `UAD Tube-Tech CL 1B`, `UAD API 2500` (for an **1176 FET or LA-2A opto** compressor inside a full 610 channel — switchable per-mode — see the LA-6176 channel strip → [[la-6176]]) | `[[vst-compress]]` / `[[multiband-compress]]` |
| **Tape / saturation / color** | `Softube Tape` (Softube's own 3-machine tape — A/B/C machines, **gain-compensated Amount** moves crest not tone, Crosstalk = *glue/narrow, not width* → measured deep-dive [[softube-tape]]; renders headless, iLok-authorized here, re-verify elsewhere; **13/15 params are string enums → preset harness only**), `UAD Studer A800` (multitrack / punchy → measured deep-dive [[studer-a800]]), `UAD Ampex ATR-102` (2-track mastering / glossy, ¼/½/1″ width + even-harmonic transformer → measured deep-dive [[ampex-atr-102]]) — both UADx `uaudio_*.vst3`, render headless & **no-iLok**; `UAD Oxide Tape Recorder` (UA's **simple/affordable/low-CPU** tape — the same ATR/A800 engine team stripped to **one Input drive + 4 switches** [Input/Repro · 7.5/15 IPS · NAB/CCIR · NR], no tape type / bias / width / transformer / 30 IPS → measured deep-dive [[oxide-tape]]; load the UADx `uaudio_oxide_tape.vst3`, renders headless & **no-iLok** [the `UAD Oxide Tape.component` twin passes through]; **`input_level` is the WHOLE color knob** — it trades crest for odd-3rd THD and, unlike the default-darkening ATR/A800, is near-neutral at low drive and **BRIGHTENS as driven** [centroid 2990→3800 over +0→+24]; **CCIR = brighter top vs NAB warmer, 15 IPS = warm/full lows vs 7.5 colored/leaner**, **NR is a noise-floor switch not a tone control**, and `path_select`/`ips`/`emphasis_eq`/`noise_reduct` are string/bool enums → harness only [only `input_level`/`output_level` are numeric/float-dict-reachable]); `Tape Machine 80`, `Tape Machine 440`, `FabFilter Saturn 2` (6-band **MULTIBAND distortion/saturation** — 28 styles [tube/tape/amp/transformer/saturation + Foldback/Rectify/Destroy/Smudge/Breakdown FX] with per-band Drive/Dynamics/Feedback/Tone/Mix, Mid-Side, Linear-phase crossovers + HQ oversampling → measured deep-dive [[fabfilter-saturn-2]]; renders headless & **no-iLok** [a 2020 plugin — don't conflate with Pro-Q 4]; **a bare load restores the last-saved GUI preset [not neutral] and `drive=0` isn't clean [only `mix=0`/bypass null], the *style* is a string enum so `apply-vst-chain`'s float dict can't pick it → harness only**; measured harmonics — Tube=even+odd [Warm=warmth], **Tape/Saturation=odd-only, no 2nd harmonic**, Amp=heavy odd, Foldback=wavefold [5th>fundamental], Rectify=octave-up [fundamental removed], Destroy=crush; **drive crushes crest → protect with the parallel `mix` or negative `dynamics`**), `UAD Vibe Analog Machines` (UA's analog **"vibe"/character** box — 10 modeled "machines" `SWEETEN`/`WARM`/`GLOW`/`THICKEN`/`EDGE`/`VINTAGIZE`/`SPUTTER`/`DISTORT`/`OVERDRIVE`/`FIRE` = harmonic SATURATION + a 2nd knob — **WARBLE** (tape wow/flutter) on the 6 tape machines or **TONE** (brightness) on the 4 preamp machines — **NOT an EQ or a real compressor** → measured deep-dive [[vibe-analog-machines]]; **the product is the renamed Verve Analog Machines** so the binary is **`uaudio_verve.vst3`** (codename "verve", reports product name "UADx Vibe Analog Machines") — UADx native, renders headless [iLok account, no dongle — authorized here; bypass == dry]; 6 params, the `machine` is a STRING enum → harness only [the float dict sets the numerics but misses the machine]; **`drive`=`param_1` 0 is NOT bypass** [use `master_bypass`/`power`], and **`param_2` is WARBLE (tape) / TONE (preamp)** — WARBLE is pitch modulation → invisible to spectrum/crest meters [audition it], TONE sweeps the centroid; Essentials = `uaudio_verve_essentials.vst3`, 4 tape machines drive-only), `Airwindows Consolidated` | `[[vst-saturate]]` (UAD tapes: [[studer-a800]] / [[ampex-atr-102]] / [[oxide-tape]] (simple/fast); Softube: [[softube-tape]]; FabFilter multiband: [[fabfilter-saturn-2]]; UAD vibe/lo-fi character: [[vibe-analog-machines]]) |
| **Reverb** | `ValhallaPlate`, `FabFilter Pro-R 2`, `SSL Native FlexVerb` | `[[vst-reverb]]` |
| **Delay / echo** | `Delay BRIGADE`, `UAD EP-34 Tape Echo`, `FabFilter Timeless 3` | `[[vst-delay]]` |
| **De-esser** | `FabFilter Pro-DS`, `SSL DeEss`, `Lindell 902 De-esser`, `De Esser` | `[[vst-de-ess]]` |
| **Limiter (master)** | `FabFilter Pro-L 2` (true-peak brickwall, 8 styles + LUFS/dBTP metering → measured deep-dive [[fabfilter-pro-l-2]]; renders headless & **no-iLok**, but a limiter owns the ceiling → render faithfully via `apply-vst-chain` + a `.state` blob, NOT the renormalizing preset harness), `Brickwall Limiter`, `TR5 Brickwall Limiter` | `[[vst-master]]` |
| **Amp / pedal (guitar/bass)** | `TONEX`, `NeuralAmpModeler`, `UAD Softube Bass Amp Room`, `UAD Softube Metal Amp Room` | `[[vst-amp]]` |
| **Transient shaping** | `Transient Shaper` (Softube — 2-band attack/sustain; renders headless, iLok-authorized here — re-verify elsewhere) | `[[softube-transient-shaper]]` (or native [[drum-punch]], no plugin) |

> Avoid (blocked here): `AIR Studios Reverb`, anything `Slate …`, `Eiosis …`, the clean `Ozone 11 …`
> VST3 entries (use FabFilter/SSL for mastering instead), the `UAD ….component` build — load the
> `uaudio_*.vst3` twin instead — and **`Tape J-37` (Arturia)**, which **passthroughs offline** (DAW-only;
> the J37 Studer tape → use it in the DAW per [[tape-j-37]], or [[studer-a800]] / [[ampex-atr-102]] for a
> headless tape). When unsure it renders, screen with `[[vst-verify]]`; grep the raw list.

## Presets

Saved, reusable chains live in [`presets/vst/`](../../presets/vst/README.md) — a portable recipe
(gain-stage → chain → narrow → trim) + byte-exact `.state` blobs, re-applicable to any file via
`presets/vst/apply_vst_preset.py`. Available: **`vintage-1960s`** (UADx Pultec → Fairchild 670 →
Ampex tape → period-mono; warm/dark/glued 1960s drums) and **`tight-70s`** (UADx Neve 1073 → dbx 160
→ Studer A800 @15 IPS; punchy/dry/present 1970s drums). Presets use the `uaudio_*.vst3` UADx build. Plus a **`softube-ts-*`** set on the Softube `Transient Shaper.vst3` (tighten-low · kick-click · snare-crack · overhead-shimmer · tame-cymbals · bus-glue — each measure-validated; see [[softube-transient-shaper]]), and **`blackbird-a5-drums`** on `KIT BB A5.vst3` (Mic-mode console drive + 55L EQ; forward/punchy "American console" drum bus — the KIT/Blackbird API counterpart to `tight-70s-api`; see [[kit-bb-a5]]), and **`bb-n105-warm-drum-bus`** on `KIT BB N105 V2.vst3` (Mic-mode Neve transformer drive + 31105 EQ weight/de-box; **warm + tight** drum bus, warmth via the transformer not EQ air; see [[kit-bb-n105]]), and **`bb-n73-warm-drum-bus`** on `KIT BB N73.vst3` (the classic 3-band **Neve 1073**: Mic-mode transformer drive + low-shelf weight + 700 Hz de-box, HF off; **warm + weighty** drum bus that adds low weight and stays *dark* — vs the N105's sheen — and **`pre_amp_saturation` MUST be ON** in Mic or the driven preamp digitally clips; `master_bus_toggle` is host-exposed here for glue; all freq/mode/switch enums MUST go through the harness; see [[kit-bb-n73]]), and a **`ssl-bc2-*`** set on `SSL Native Bus Compressor 2.vst3` (drum-glue · mix-glue · parallel-smash — VCA bus "glue"; ratio/release/SC-HPF are string enums so they MUST go through the harness, not `apply-vst-chain`; see [[ssl-bus-compressor-2]]), and a **`ssl-4k-e-*`** set on `SSL 4K E.vst3` (drum-bus = 242-Black EQ weight + slow-attack VCA punch; glue = 02-Brown gentle 2:1 — the clean/weighty British channel; **every string enum (ratio/eq_colour/types/routing) MUST go through the harness**, the float dict silently drops them; see [[ssl-4k-e]]), and an **`ampex-atr-*`** set on `uaudio_ampex_atr-102_tape.vst3` (master-glue = 30 IPS/1″ transparent 2-bus polish, crest ~untouched · warm-2bus = 15 IPS/½″ NAB fuller mixdown · drum-glue = driven 15 IPS smooth tape glue; the 2-track mastering/glossy tape, even-harmonic transformer ON — every speed/tape/cal/width is an enum so the **full set + `auto_cal` MUST go through the harness** or repro-EQ stays mis-calibrated; see [[ampex-atr-102]]), and a **`softube-tape-*`** set on the Softube `Tape.vst3` (drum-glue-warm = Type B/15 IPS warm-fat glue · drum-tight = Type A/30 IPS tight · mix-bus-glue = Type A/30 IPS transparent · parallel-thick = Type C driven + 50% blend · lofi = slow/wobble/noise — Softube's 3-machine tape; **Amount is gain-compensated so it moves crest not tone**, Crosstalk glues/narrows rather than widens, and every string enum MUST go through the harness, not `apply-vst-chain`; see [[softube-tape]]), and an **`oxide-*`** set on `uaudio_oxide_tape.vst3` (warm-drum-glue = Repro/15/NAB/NR-on, Input **+7** → warm + LF weight + ~1 dB glue, crest 14.6→13.7 · master-glue = 15/NAB gentle Input **+4** → near-transparent 2-bus polish before the limiter · lofi-color = **7.5**/NAB/NR-off, Input **+14** → odd-3rd grit (~7–10% THD) + tape compression, crest →~12; UA's simple Oxide tape — **`input_level` is the whole color knob (drive = saturation + compression + level, no Auto-Gain → trim `output_level`), it BRIGHTENS as driven not darkens, CCIR is the brighter EQ and 7.5 IPS the colored speed, and `path_select`/`ips`/`emphasis_eq`/`noise_reduct` are string/bool enums → harness only**; see [[oxide-tape]]), and a **`helios-type-69-*`** set on `uaudio_helios_type_69.vst3` (warm-drum-glue = Mic g40 + −20 pad + 4 dB 10k air → even-harmonic preamp drive, crest 14.6→13.1 · clean-air-tighten = Line, +4 air + −3 bass-cut + 700 Hz mid-Trough → pure EQ, crest 14.6→16.0; the warm British Helios preamp+EQ — **all 14 params are enums, the bass BOOST is unreachable headless (`bass_gain` locked Off) so only the cut renders, and Peak/Trough mid-gain is nonlinear → harness only**; see [[helios-type-69]]), and a **`fc660-*`** set on `uaudio_fairchild_660.vst3` (drum-color = in −6 / thr 6 / **TC 4** / SC 60 Hz → tube color + glue that keeps punch, crest 17.17→16.90 · LRA 2.44→1.84 · low-mid ratio 0.179→0.188 · low-band punch held · bus-glue = TC 5 program-dependent auto · parallel-smash = in 0 / thr 8 / **TC 1** / **MIX 40** / HR 12 → bigger/denser, +1.81 LU; the vari-mu Fairchild 660 — **colors more than it crushes** (crest ~17–19 across input/threshold), **TC 4 = most open/punchy, TC 1 = tightest, TC 5/6 = auto**, SC filter takes lows out of the detector, **all 12 params are enums and threshold/HR measure inverted to the dial lore → harness only**; see [[fairchild-660]]), and a **`hitsville-*`** set on `uaudio_hitsville_eq_mastering.vst3` (motown-master = L/R 50 Pk3 + 130 Pk2 + 320 Dip2 + 5k Pk2 + 12.5k Pk2 → fuller lows + air, band_ratio_low 0.589→0.722 · ms-master-width = **M-S** Mid 50 Pk2/320 Dip2/2k Pk1 + Side 130 Dip2/5k Pk3/12.5k Pk3 → mono-tight lows (low width −30→−33 dB) + wider top (high −19.8→−15.3), corr 0.981→0.986 · drum-bus-weight = 130 Pk3 + 800 Dip2 + 5k Pk2 + 12.5k Pk1; the Motown / Hitsville disk-mastering EQ — broad passive graphic EQ + TRUE Mid/Side, **the "8" knob ≈ +5/−3 dB not dB and is asymmetric, ALL 53 params are enums, the global `*_speed` switch is INERT headless (set per-band `lN_freq`), and M-S needs `ctrl_link='Unlink'` → harness only**; see [[hitsville-eq-mastering]]), and a **`hitsville-eq-*`** set on the single-channel `uaudio_hitsville_eq.vst3` **Studio EQ** (motown-drum-bus = 50 +3 / 320 −2 / 5k +2 / 12.5k +2, gain −2 → forward Motown drums, low 0.542→0.661 + de-boxed low-mids 0.378→0.248 + centroid 3128→3531, crest kept 23.6→22.9 · warm-drum-glue = same weight+de-box, top restrained → centroid **3129 ≈ dry** (warm not bright) · mix-glue = gentle bus/master tilt; the Motown 7-band graphic EQ — **a clean 0 %-THD EQ whose only distortion is the GAIN makeup clipping past 0 dBFS**, proportional-Q (peak:octave 1.1→3.5 at +2→+8), `bypass`=In/Out/Off (Out keeps the transformer/amp colour) — and **the 7 band gains + GAIN are numeric so this preset set ALSO sets cleanly via `apply-vst-chain`'s float dict**, the harness only adds the gain-stage/peak-trim; see [[hitsville-eq]]), and a **`pultec-hlf-3c-*`** set on `uaudio_pultec_hlf-3c.vst3` (drum-bus-shape = 50 CPS low-cut + 15 KCS high-cut → gentle musical band-limit, crest 14.6→15.9 · tame-harsh-top = 10 KCS high-cut only → smooth top rolloff, centroid 1593→1324 with the body untouched · lofi-bandpass = 250 CPS / 3 KCS → telephone band, lows gutted 0.80→0.01, crest 14.6→24.1; the Pultec **passive FILTER** — **a pure 0.000 %-THD filter (it only subtracts — colour elsewhere), ~18 dB/oct slopes whose labels read ~−4 dB (low-cut) / are gentlest at 15 KCS (high-cut), all 4 string/bool enums → harness only, and the standard `probe_plugin.py` FALSE-flags it (no gain param) → verify with a real corner**; see [[pultec-hlf-3c]]), and a **`pultec-*`** set on `uaudio_pultec_eqp-1a.vst3` (drum-lowend-glue = 60 CPS B5/A4 **low-end trick** + 5 KCS de-harsh + 16 KCS sheen → warm + TIGHT drums, low ratio 0.80→0.88 with a low-mid scoop 0.18→0.12 and centroid 1593→1231, **crest HELD** 14.6→13.4 · air-without-fizz = 16 KCS boost + 10 KCS cut → +1.5 dB @16k air while 5–8k drops, crest held · master-smile = gentle 30 CPS trick + 16 KCS air, sits *before* the limiter; the Pultec passive LC + tube **program EQ** — **the boost/atten/bandwidth knobs are 0–10 positions not dB & nonlinear, the colour is the CURVES not drive (~0.002 % THD @−18 dBFS, ~0.01 % @−6), engaged-flat is never a true null (+1.1 dB makeup ≈ UA's ~1.13 dB → loudness-match), `enable=Out` keeps the amp path (brighter), and `low_freq`/`high_freq`/`hf_atten_freq`/`enable`/`output` are string enums → harness only**; see [[pultec-eqp-1a]]), and a **`pultec-meq5-*`** set on `uaudio_pultec_meq-5.vst3` (drum-presence = LOW PEAK 200 @3 + DIP 500 @4 + HIGH PEAK 3k @5 → forward attack + de-box, centroid 2223→2261, high-mid 0.035→0.051 · mid-scoop = DIP 700 @5 + HP 4k @3 + LP 200 @2 → the signature de-honk/de-box, mid 0.036→0.011 · warm-body = LOW PEAK 300 @4 + DIP 2k @4 + HP 5k @4 → low-mid warmth, low-mid 0.327→0.433 + centroid 2223→2069, crest held; the **MIDRANGE** Pultec (200 Hz–7 kHz: LOW PEAK boost / DIP cut / HIGH PEAK boost — the mid box between the EQP-1A's lows+air and the HLF-3C filters) — **unlike the EQP-1A/Hitsville knobs the MEQ-5 0–10 dial is ROUGHLY dB** (LOW PEAK ~+1/unit→+10.7 max, HIGH PEAK ~+0.9→+8.8, the DIP saturates ~−11 dB by ~7), **boost + DIP at the same freq FOCUSES not cancels** (700@10+700@10 = +2.3 dB), and the **three amount knobs `lm_peak`/`mid_dip`/`hm_peak` DO set via the float dict but the freq selectors + `output` + `enable` are string enums → harness only** (a float-dict build mis-places LOW/HIGH PEAK to the defaults lm 200/hm 1.5k); see [[pultec-meq-5]]), and a **`fabfilter-promb-*`** set on `FabFilter Pro-MB.vst3` (drum-deharsh = one Compression band 3–9 kHz / **range −9** / 4:1 / Dynamic Phase → ducks harsh presence only on its peaks, measured 5 kHz −4.7 / 4 kHz −4.0 dB while 1 kHz −0.05 & lows untouched, centroid 1593→1506 · drum-multiband-glue = 3 bands (lows 30–120 + low-mid box 200–600 + presence 3–9k), each gentle Compression; the 6-band multiband dynamics — **every band needs `band_N_state=Enabled` AND a NONZERO `range` (range 0 = silent no-op), a bare load is already a passthrough so NO flatten step, and all 156 params are enums (`state`/`dynamics_mode`/`ratio` strings) → harness only, the float dict can't enable a band**; see [[fabfilter-pro-mb]]), and a **`saturn2-*`** set on `FabFilter Saturn 2.vst3` (drum-bus-warm = 1 band Warm Tube drive 24 + **dynamics −0.3** to keep punch + warm tone → warm/tight glue, crest 14.6→**13.8**, centroid 3007→2526, +3 dB denser · parallel-smash = British Rock Amp drive 45 at **global `mix` 30** → amp grit *under* the dry hits, crest **preserved** 14.6→14.9 · bass-multiband = 2 bands @110 Hz, **sub band `mix=0` [clean]**, highs Warm Tube drive 45 + presence → translation harmonics, centroid 3007→3479 with the sub untouched; the 6-band multiband distortion/saturation — **a bare load restores the last GUI preset [NOT neutral] and `drive=0` isn't clean [only `mix=0`/bypass null], the *style* is a string enum so the float dict can't pick it, drive crushes crest [protect with the parallel `mix` or negative `dynamics`] → harness only**; see [[fabfilter-saturn-2]]), and a **`vibe-*`** set on `uaudio_verve.vst3` (the UADx **Vibe Analog Machines**) — sweeten-drum-glue = `SWEETEN` machine, drive 50 → clean BRIGHTEN/air with the **crest HELD** 14.6→14.8 (centroid 1593→1725, an exciter-like sweeten) · warm-drum-bus = `WARM` machine, drive 65 → warm + glued, crest 14.6→**12.1**, centroid 1593→1335, top *gently* rolled (matches the user's warm+tight drum-bus preference) · lofi-warble = `VINTAGIZE` machine, drive 50 + **warble 35** → dark/band-limited lo-fi + tape **wow/flutter** (warble is pitch modulation, audible-only — invisible to the meters); the 10-machine analog vibe/saturation box — **`machine` is a STRING enum → harness only [the float dict misses it], `drive` (param_1) 0 is NOT bypass, and `warble` (param_2) won't show in `measure-spectrum`**; see [[vibe-analog-machines]]), and a **`voxbox-*`** set on `uaudio_manley_voxbox.vst3` (the all-tube Manley VOXBOX channel) — vocal-channel = Mic + 80 Hz HPF + opto comp (thr9 Med/Med-Slow) + Pultec body/de-honk/air (lo 3@100 / mid −3@700 / hi 5@12k) + de-ess 6K (the canonical vocal chain) · drum-glue = Mic in4 g50 drive + comp thr8 + Pultec lo 5@70 / hi 5@10k / mid −3@500 → warm/rounded, the **opto comp OPENS transients so crest 14.6→19.5** (not glue-squash), centroid 1593→1482 · bus-tube-glue = Line in4 g40 tube tone + Pultec smile (lo 3@50 / hi 4@16k) + the de-ess's **`Limit` opto limiter** (thr5) → crest 14.6→12.8, +4 dB denser; the smooth/open tube vocal channel — **the audio path is comp-FIRST → pre → EQ → de-ess (NOT the GUI order), all 25 params are enums and the string switches (source/HPF/the In engages/attack/release/de-ess-band/transformer) MUST go through the harness, the comp ratio is program-dependent (nominal 3:1) with thresholds RELATIVE to `input` (higher = more), the Pultec dials are nonlinear PEAK-DIP-PEAK (no cuts/Q, HI band incl. 2 kHz), and `Limit` is a smooth opto limiter not a brickwall**; see [[manley-voxbox]]), and a **`la6176-*`** set on `uaudio_la_6176.vst3` (warm-drum-glue = Line g+5 even-harmonic tube warmth + low 100 +3 / 10k +1.5 + a **PARALLEL 1176** [4:1, slow attack, mix 6, `sc_filt` on] → warm + tight, crest 14.6→12.1, centroid 1593→1405, low ratio 0.798→0.848 · la2a-drum-punch = clean Line + 7k +3 air + **LA-2A** Comp pk6 → the opto-on-drums crest RISE 14.6→16.1 · vocal-strip = Line + 75 Hz cut + 10k +3 air + LA-2A Comp pk4 smooth leveling, the classic 6176 vocal chain; the **610 preamp + 2-band shelf EQ + switchable 1176/LA-2A** channel strip — **a bare load already compresses [bypass for a clean A/B], the Mic path is a high-gain mic pre (~40 % THD on a line stem → use Line), the EQ feeds the compressor, the inactive mode's controls go inert, and all 26 params are enums incl. digit-prefixed `1176_*` → harness only**; see [[la-6176]]), and a **`dbx160-*`** set on `uaudio_dbx_160.vst3` (drum-punch ★ = 4:1 / thr −24 → **ADDS punch, crest 17.17→19.29** with the kick tightened [low-band crest 17.1→15.0] + fuller low-mid 0.179→0.204, LRA 2.44→1.40 · bus-glue = gentle 2:1 / thr −22 → leveling, crest 17.17→**16.69** [DOWN] + LRA→1.88 · parallel-punch = Inf:1 / thr −30 / **mix 50** → density/weight +0.44 LU at the same peak, tone preserved; UA's model of the 1976 dbx 160 "VU" — a feed-forward true-RMS **hard-knee Blackmer-VCA** that on drums **RAISES crest** [the ratio is a crest dial: ≤2:1 levels, 4:1+ punches, ∞:1 hard-limits], an exceptionally **clean VCA** [THD ~0.04 % working — character is dynamic not harmonic], with **no attack/release** [program-dependent RMS]; **the ratio `compress` is a STRING enum (`' 4.0:1'`/`'Inf:1'`) + `meter`/`sc_filter` are string/bool → harness only** [only thresh/gain/mix are numeric], `sc_filter` (PULL/SC) is a detector HPF that INVERTS it [crest down + bassier = de-harsh not punch], and it models the hard-knee VU [no OverEasy, no negative/INFINITY+]; see [[dbx-160]]), and a **`massive-passive-*`** set on the **Manley Massive Passive** (3 on the **standard** `uaudio_manley_massive_passive.vst3` + 1 on the MST `…_m.vst3`) — drum-bus = wide low SHELF @68 + low-mid BELL CUT @270 + 12k air SHELF → weight + tight low-mids + air (low 0.798→0.856, low-mid 0.176→0.128, high-mid 0.0113→0.0076, centroid 1593→1849, **crest HELD** 14.6→14.6) · low-end-trick = the **LOW-END TRICK** (low SHELF @47 boost + low-mid BELL CUT @180 — two bands, since one can't boost+cut) → big-but-tight (low 0.798→0.847, low-mid 0.176→0.131, top untouched) · air-deharsh = 16k air SHELF + a 3.3k hi-mid BELL CUT → top up (high 0.0043→**0.0061**) **while the harsh presence drops** (high-mid 0.0113→0.0056, centroid 1593→2038) · master-polish = the **MST** mastering build, a gentle 2-bus low SHELF @47 + 390 de-mud + 16k air (low 0.798→0.829, low-mid 0.176→0.153, **crest HELD** 14.6→14.1, before the limiter); the 4-band **PARALLEL passive (LC inductor) + all-tube make-up EQ** — **every band defaults to `OUT` (a bare load + `probe_plugin.py` BOTH read passthrough → engage a band + verify with a real boost), the GAIN dial is nonlinear + BANDWIDTH-COUPLED (dB = gain × bw; `bw` 1.0 widest → 3.0 narrowest/tallest), a SHELF + narrow `bw` grows a resonant corner, the bands INTERACT (non-additive), it's a CLEAN EQ (≤0.25 % THD), and all 51 params are enums (`enable`/`shape`/`lopass`/`hipass`/`ctrllink` strings) → harness only, set BOTH channels**; see [[manley-massive-passive]]). The harness (`apply_vst_preset.py`) now snaps log-stepped string-enum corners (e.g. an SSL HPF) to the nearest valid value, and `presets/vst/dump_params.py` dumps any plugin's full param surface (sibling to `probe_plugin.py`).

## Refresh

The inventory is a snapshot. To regenerate after installing/authorizing plugins, run `[[vst-browse]]`
(which calls `[L] list-vst-plugins` live) and re-run the load-probe that produced the two `demo/`
files. Counts as of the last scan: **779 headless-safe** of 944 loadable (4,883 bundles on disk).
